import React from 'react';
import PageWrapper from '../../components/common/PageWrapper';

const AssignedDeliveries = () => (
  <PageWrapper>
    <h1 style={{ fontSize: '2rem', fontWeight: 800, color: '#fff', marginBottom: 8 }}>📋 Assigned Deliveries</h1>
    <p style={{ color: '#94a3b8' }}>View and update your assigned deliveries</p>
  </PageWrapper>
);
export default AssignedDeliveries;